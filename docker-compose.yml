version: '3.7'

services:

  react-template:
    container_name: bookbnb-react
    build:
      context: ./
      dockerfile: docker/Dockerfile
    volumes:
    - ./:/app
    ports:
    - 3000:3000
    environment:
    - CHOKIDAR_USEPOLLING=true
    stdin_open: true